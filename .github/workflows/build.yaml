name: Build
on:
  push:
    branches: "*"

jobs:
  linux-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: sudo apt-get install libglfw3 libglfw3-dev cmake
      - name: build
        run: make
