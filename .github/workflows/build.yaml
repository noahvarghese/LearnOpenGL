name: Build
on:
  push:
    branches: 
      - "*"

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: sudo apt-get install libglfw3 libglfw3-dev cmake libx11-dev libxi-dev
      - name: build
        run: make
